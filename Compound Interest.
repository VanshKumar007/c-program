#include <stdio.h>
#include <math.h>

int main() {
    int P, T;
    float R, CI;

    scanf("%d", &P);  
    scanf("%f", &R);  
    scanf("%d", &T);   

    CI = P * (pow((1 + R/100), T)) - P;

    printf("%.2f", CI);

    return 0;
}
