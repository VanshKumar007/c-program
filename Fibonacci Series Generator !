#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);

    int a = 0, b = 1;  

    for(int i = 0; i < n; i++) {
        printf("%d", a);         
        if(i < n - 1) printf(" "); 
        int next = a + b;      
        a = b;
        b = next;
    }

    return 0;
}
