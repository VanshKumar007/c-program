#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, a[100];
    scanf("%d", &n);

    for(int i = 0; i < n; i++)
        scanf("%d", &a[i]);

    int found = 0; // ye check karega koi unique mila ya nahi

    for(int i = 0; i < n; i++) {
        int count = 0;

        for(int j = 0; j < n; j++) {
            if(a[i] == a[j])
                count++;
        }

        if(count == 1) {
            printf("%d ", a[i]);
            found = 1;  // unique mila
        }
    }

    if(found == 0)
        printf("No unique elements");

    return 0;
}
