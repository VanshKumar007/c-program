#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, a[100];
    scanf("%d", &n);

    for (int i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("%d ", a[0]);  // first element print

    for (int i = 1; i < n; i++) {
        int isDuplicate = 0;
        for (int j = 0; j < i; j++) {
            if (a[i] == a[j]) {
                isDuplicate = 1;
                break;
            }
        }
        if (isDuplicate == 0)
            printf("%d ", a[i]);
    }

    return 0;
}
